// LearningControls - 暂时禁用，新的 Card-Driven Learning Flow 使用 LearningProgress.tsx
// TODO: Remove or refactor when new learning flow is complete

export function LearningControls() {
    // 暂时返回空，避免引用不存在的属性
    return null;
}
